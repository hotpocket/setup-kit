# OPT-IN (cond_virtualbox=yes in the host conf): ships a dkms kernel module + core system changes, so bare metal is not consent. Even when opted in, skipped on a proxmox-host or inside any VM (systemd-detect-virt). Plain 'virtualbox' from multiverse — NOT Oracle's versioned virtualbox-7.2 (needs their repo; fails on fresh boxes)
#
virtualbox                                  #    106.5 MB  contrib/misc
