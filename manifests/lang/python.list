# Python — ONE stable interpreter for a base install (one version per
# language kit-wide, a step or two behind latest for stability). 3.12 aligns
# with .configs setup.sh (PYTHON_VERSION=3.12.11). Prefix resolves to the
# latest 3.12.x at install time. Project envs rebuild per-project.
3.12

# pipx tools
pipx:poetry
